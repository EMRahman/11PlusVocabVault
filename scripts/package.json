{
  "//": "These data-generation build scripts are written as CommonJS (require/module.exports). The root package.json sets \"type\": \"module\" for the app and tests, so this nested manifest keeps this directory CommonJS.",
  "type": "commonjs"
}
